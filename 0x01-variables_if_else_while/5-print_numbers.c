#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 1
 */

int main(void)
{
	int number;

	for (number = 0; number <= 9; number++)
	{
		printf("%d", number);
	}
	putchar(number);

	return (0);
}
