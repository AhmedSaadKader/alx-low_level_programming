#include "main.h"

/**
 * print_chessboard - prints the chessboard
 * @a: board array
 */

void print_chessboard(char (*a)[8])
{
	int i, j, board_dimensions;

	board_dimensions = 8;

	for (i = 0; i < 8; i++)
	{
	  _putchar(a[i]);
	}
}
